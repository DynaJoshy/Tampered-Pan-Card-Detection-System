A simple web-app using Flask which can detect the accuracy to which a PAN card is tampered.<br/>
### Steps

 **Clone the repository**:
   ```bash
   git clone git clone https://github.com/DynaJoshy/pan-card-tamper-detection.git
   
### Run the application

